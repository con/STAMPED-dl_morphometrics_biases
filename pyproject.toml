[project]
name = "stamped-dl-morphometrics-biases"
version = "0.0.0"
description = "Ideal-oriented STAMPED reconstruction of morphometrics bias analyses"
requires-python = ">=3.12"

[tool.pytest.ini_options]
addopts = "-ra"
testpaths = ["tests"]
