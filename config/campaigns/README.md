# Prospective campaign configurations

A campaign composes one exact dataset, pipeline/runtime, input condition, and
cluster profile. Before initialization it must satisfy
`config/schemas/campaign.schema.json`. Resolved state and commands belong in the
independent operations dataset named by the campaign.
