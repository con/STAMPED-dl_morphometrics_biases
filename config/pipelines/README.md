# Pipeline records

Each record declares scientific arguments, the exact accepted SIF identity,
inputs, outputs, seeds, and failure behavior. Candidate or unresolved runtimes
must be labelled as such.
