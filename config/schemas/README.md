# Record schemas

JSON Schema Draft 2020-12 contracts define prospective campaigns, image records,
result-manifest rows, and append-only operations events. The schemas are
validated through the locked Phase 1 quality environment.
