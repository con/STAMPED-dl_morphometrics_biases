# Dataset records

Each record identifies a dataset, its exact version or unresolved identity,
access class, cohort policy, governing terms, retrieval locations, and
distribution decision. Credentials and prohibited participant metadata are
never stored here.
