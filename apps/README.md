# Authored BIDS Apps

This directory is the authority for project-authored BIDS App entrypoints.
It is intentionally empty during Phase 1. Each future app must follow the
repository-local BIDS App builder skill and expose the standard BIDS App
interface from its registered SIF.
